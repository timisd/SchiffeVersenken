<UserControl x:Class="Battleships.MVVM.Views.OptionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:navigation="clr-namespace:Battleships.Services.Navigation"
             xmlns:viewModels="clr-namespace:Battleships.MVVM.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
        <Label FontSize="50" 
               Content="Optionen"/>
        <Button FontSize="30"
                Content="Hauptmenü"
                d:DataContext="{d:DesignInstance viewModels:OptionsViewModel}"
                Command="{Binding UpdateCurrentViewModelCommand}"
                CommandParameter="{x:Static navigation:ViewsEnum.Menu}"/>
    </StackPanel>
</UserControl>
